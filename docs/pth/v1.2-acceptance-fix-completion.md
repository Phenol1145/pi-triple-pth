# v1.2 验收阻塞修复完成记录（F1–F5）

> 输入：`v1.2-acceptance-evaluation-report.md`（NOT ACCEPTED）。
> 修复计划：`n24-v12-acceptance-fix-plan.md`；分契约 `n24-f1..f5-contract.md`。
> 本文只记录阻塞项 → 修复提交 → 验证证据的映射。

| 阻塞项 | 修复 | 关键内容 | 验证 |
|---|---|---|---|
| AB-02 provenance 布局 | F1 `c717677` | canonical=`meta.provenance`；store 门禁只认嵌套；seed 移除平铺 workaround；真实 PG domain-fact 晋升链路测试 | 全量绿 |
| AB-03 capability 覆盖 | F1 `c717677` | runner 合并 `caps.knowledge` 与 `{context}`；adversarial/memory-keeper 组合测试 | 全量绿 |
| 6.3 revision/promotion | F1 `c717677` | update 事务化记 revision（幂等 no-op 不记）；promote 幂等重放 | 真实 PG 测试 |
| AB-01 租户复合身份 | F2 `d6f6487` | `(tenant_id,id)` PK 迁移；requireTenant fail-closed（生产装配开启）；raw query 三层门禁（broker memory.query / grant 映射 / bridge admin-only）；同 id 跨租户并存+并发测试 | 真实 PG 测试 |
| AB-04 主体与 RBAC | F3 `8d79538` | verdict 增 principalId/executionId/domainId/candidateRevision/evidence；canPromote 主体分离；verify/promote 仅 platform-admin；worker 能力 principal 盖章 | 路由/能力测试 |
| AB-05 delegate 域丢失 | F3 `8d79538` | TaskDispatchContext 携带 domains/binding；delegate 默认继承、显式子集校验；payload 服务端盖章 | task-control 测试 |
| AB-06 评测专用 Catalog | F4 `aa0aa34` | aliases 烘焙进生产 DISCIPLINE_DEFINITIONS；删除 buildPilotCatalog；computeVersion 覆盖行为字段；生产/评测同一 catalog | 生产 Catalog 60 题 recall=1.0 |
| AB-07 固定 top-5 | F4 `aa0aa34` | `knowledge-ranking.ts`：domainRelevance + queryTokenHits；broker/context/evaluator 三方共用 | ranking 测试 + 84 题 |
| AB-08 空 evidence 满分 | F4 `aa0aa34` | authoritative fail-closed（非空 top5 + 非空 evidence + sourceId 存在）；live 只读 DB meta.evidence | 84 题全过 |
| 6.4 source hash | F4 `aa0aa34` | registryFingerprint 与 artifactHash 分离；snapshot 内容 + sha256 校验 | 数据测试 |
| 6.5 EvidenceRef 丢失 | F4 `aa0aa34` | seed 写结构化 meta.evidence（sourceId+locator） | live 评测 |
| 6.6 题型不足 | F4 `aa0aa34` | 84 题：60 标准 + 12 hard negative + 8 多域 + 4 混淆；新指标 3 项全 1.0 | 评测器测试 |
| 6.1 outbox | F5 `32d2efa` | `side_effect_outbox` 表 + PgSideEffectOutbox + drainer；refine observer 同步 enqueue；claim 前 drain；attempts≥3 failed 留审计 | fake 重放测试 |
| 6.2 candidate lineage | F5 `32d2efa` | RefineInput/scopedMeta 增 domains/binding/outcome/artifactRefs；observer 与 legacy 双路径传入 | refiner 测试 |
| 6.7 audit 绑定 | F5 `32d2efa` | 捕获 store 对象后对象方法调用；audit/transcripts 绑定测试 | task-loop 测试 |

## 最终验证

- 全量 vitest：F5 后 main 实跑（见根 TODO 的最新数字）；
- `npm run lint`：tsc/packages/boundaries/config 全绿；
- K5 评测（生产 Catalog + 生产 ranking + DB evidence）：84/84 pass，
  domainRecallAt3 / knowledgeRecallAt5 / evidenceCoverage / hardNegativePassRate /
  multiDomainResolution / distractorTop3Rate 均为 1.0（离线与 live）。

## 遗留边界（已声明，非本轮阻塞）

- `bridgeQuery` 的 raw SQL 本身仍只允许 platform-admin；未做 SQL 层自动租户谓词改写。
- source snapshot 是人工摘录快照，不是远程原文全量镜像（artifactHash 对快照内容成立）。
- 跨租户真实任务未重跑，仅真实 PG 自动化负向测试。
