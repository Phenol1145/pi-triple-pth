# v1.2 验收阻塞修复最终复验报告

> 日期：2026-08-18  
> 复验对象：`main@c2c0729`（R1–R5 已全部合并；R6 已由 lane tip `4d0a38b` 经 merge commit `c2c0729` 合入 main——**R1–R6 全部 merged**）  
> 报告生成时点：R6 验收车道（lane tip `4d0a38b`，与合并后 main 代码面一致）；本行为 N28 Gate 0 程序性补录 main commit，不改变任何验收结论或证据。  
> 输入报告：[v1.2-acceptance-fix-revalidation.md](./v1.2-acceptance-fix-revalidation.md)  
> 修复计划：[n27-r1-r6-fix-plan.md](./n27-r1-r6-fix-plan.md)  
> R6 分契约：[n27-r6-contract.md](./n27-r6-contract.md)

## 1. 结论

**ACCEPTED。**

P0-1…P0-5 与 P1-1…P1-5 逐项均为 **PASS**；R6 组合验收在真实 PostgreSQL
（testcontainers `postgres:16-alpine`）跑通完整
`claim → context → commit → outbox → candidate → verification → promotion → retrieve`
并完成七类故障注入负向断言。本轮全量 vitest、lint、K5 离线/live、R6 组合套件均重新执行，
未引用旧数字；全量测试无 `observer failed` 输出。

> 本轮 R6 是验收车道：不夹带缺陷修复；发现缺陷记录并报告（本轮未发现新缺陷）。

## 2. 验收矩阵（逐项 P0/P1）

| 项 | 状态 | 证据（测试名 / 探针输出 / 文件行号） |
|---|---|---|
| **P0-1** revision/promotion 正确性 | **PASS** | `packages/pth-memory/test/memory-store-pg.test.ts:140`（status/meta-only write 记 revision 且 version+1）、`:161`（三次连续 mutation revisions=[1,2] 且无 23505）、`:188`（幂等重放不写历史）；`test/pth-execution/knowledge-promotion.test.ts:470`（expectedRevision CAS）；R6 组合套件 `test/pth-composition/r6-acceptance.test.ts:244`（promotion 后 version=2、revisionHistory=[1]）与 `:977`（stale verdict：修改后 expectedRevision CAS 拒绝晋升） |
| **P0-2** tenant 查询面 | **PASS** | `test/pth-execution/knowledge-broker.pg.test.ts:95/110/125`（raw query 数据面租户/status/space 隔离）；R6 组合套件 `test/pth-composition/r6-acceptance.test.ts:842`（tenant B raw query 只回 tenant B 行 + retrieve 双负向） |
| **P0-3** stale verdict 失活 | **PASS** | `test/pth-execution/knowledge-promotion.test.ts:362`（stale/future expectedCandidateRevision 一律拒绝）；R6 组合套件 `test/pth-composition/r6-acceptance.test.ts:977`（计划创建后修改 candidate：旧 plan/verdict 不能晋升新版本，fail-closed） |
| **P0-4** 真事务 outbox | **PASS** | `test/pth-tasking/side-effect-outbox.test.ts:238`（enqueue 失败 → task commit 整体回滚；重放成功）；R6 组合套件 `test/pth-composition/r6-acceptance.test.ts:573`（crash between commit and enqueue：回滚后可修复，candidate 不永久缺失） |
| **P0-5** outbox 原子 claim | **PASS** | `test/pth-tasking/side-effect-outbox.test.ts:73`（双连接并发 claim 无重复）、`:194`（lease 过期回收 attempts 递增）、`:111`（stale handler 不能把 done 改回 pending）；R6 组合套件 `test/pth-composition/r6-acceptance.test.ts:623/645/670`（dual drainer / lease expiry / stale worker 三类负向重跑） |
| **P1-1** Domain 子集 binding | **PASS** | `test/pth-tasking/task-control-service.test.ts:233`（publish(A,B)→delegate(A)→claim 只保留 A 的 binding）；R6 组合套件 `test/pth-composition/r6-acceptance.test.ts:244`（生产 resolver 盖章 domains/domainBinding → claim reader 同口径解析） |
| **P1-2** VerificationPlan + service 授权 | **PASS** | `test/pth-execution/knowledge-promotion.test.ts:352`（无 auth context 拒绝）、`:362`（planId/checkId 绑定）、`:387`（晋升只读持久 verdict rows，不读 meta.verdicts）；R6 组合套件 `test/pth-composition/r6-acceptance.test.ts:244`（持久 plan + domain/adversarial 不同 principal + plan 状态 satisfied + promotion CAS 全链） |
| **P1-3** 生产端口评测 + 全语料覆盖 | **PASS** | 本轮 K5 离线/live 探针：`queryCount=138`、`coveredEntries=24/24`、`domainRecallAt3=1.0000`、`knowledgeRecallAt5=1.0000`、`evidenceCoverage=1.0000`、`hardNegativePassRate=1.0000`、`noAnswerAbstention=1.0000`、`mutationScore=1.0000`（24/24 检测）；R6 组合套件 `test/pth-composition/r6-acceptance.test.ts:244` 的生产 KnowledgeContext 注入与 `:546` 的生产 Broker search 命中 |
| **P1-4** EvidenceRef 全链一致 | **PASS** | `test/pth-runner/knowledge-context.test.ts:195`（context entries carry structured KnowledgeEvidenceRef[]）；R6 组合套件 `test/pth-composition/r6-acceptance.test.ts:244`（seed evidence → Context evidence 结构化 → candidate meta.evidence → plan.sourceBindingsDigest → promotion 全链同一形状） |
| **P1-5** observer 命名 + durable failure | **PASS** | `test/pth-tasking/task-outcome-observers.test.ts:142`（snapshot 失败入队 `observer-failure` durable record）；本轮全量 vitest 输出 `observer failed` 命中 0 次；R6 组合套件 crash 负向未产生 observer warning |

## 3. R6 组合验收证据

套件：`test/pth-composition/r6-acceptance.test.ts`（真实 PostgreSQL testcontainers，非 skip）。

| 场景 | 断言 |
|---|---|
| §4.1 全链（`:244`） | 发布 `domains=[programming-languages]` 生产 resolver 盖章 → claim 真实 lease/work.domains → KnowledgeContext 命中 official 种子且 `evidence` 为结构化 `KnowledgeEvidenceRef[]`（dev/private 不可见）→ 执行完成 → commit 与 side_effect_outbox 同事务落库（task=completed + outbox=pending）→ drainer 消费 → scoped draft candidate（`spaceScope.visibility=private` + `provenance` + 结构化 `evidence`）→ 持久 VerificationPlan（candidateRevision=1/candidateHash/sourceBindingsDigest）→ domain verdict（principal `tenant:tenant-a:domain-verifier`）+ adversarial verdict（principal `worker:controller:adversarial`）→ plan.status=satisfied → promotion（R1 单事务 CAS + R3 plan 绑定 + promotion-index outbox 同事务）→ official（version=2, promotion meta, revisionHistory=[1]）→ 生产 KnowledgeBroker search/get 命中 |
| §4.2 crash between commit and enqueue（`:573`） | 注入 enqueue 失败 → task commit 回滚（status 仍 claimed，outbox 0 行）；同 lease 重放成功 → task completed + outbox pending 同事务，candidate 不永久缺失 |
| §4.2 dual drainer（`:623`） | 两个独立 Pool 并发各 claim 10 行：共 20 行无重复、全部 processing、无 pending 可再领 |
| §4.2 lease expiry（`:645`） | claim 后不 complete，`locked_until` 过期 → 被第二个 drainer 重新 claim，attempts 1→2，行不丢 |
| §4.2 duplicate result（`:703`） | 同 key 重复 enqueue 仅 1 行；drainer 只处理 1 次；同内容重复 write version 不递增；同 promoter 重复 promotion 幂等重放（revisionHistory 仅 [1]、promotion-index 仅 1 行） |
| §4.2 stale worker（`:670`） | 旧 token complete 失败；新 owner complete 后旧 token markFailed 仍 CAS 拒绝，status 保持 done |
| §4.2 cross-tenant（`:842`） | tenant B store.get/retrieve 读不到 tenant A candidate；raw query 数据面只回 tenant B 行（JS 过滤关闭）；Broker search 不返回 tenant A 候选；recordKnowledgeVerdict/promoteKnowledgeEntry 在 tenant B 均 `not found`，tenant A candidate 仍 draft |
| §4.2 stale verdict（`:977`） | 计划创建后修改 candidate 内容（version 1→2）：旧 expectedRevision 1 promotion 被 CAS 拒绝；plan 改绑 revision 2/hashV2 后旧 verdict 行仍因 provenance/hash 失配 fail-closed 拒绝晋升 |

## 4. 新鲜验证证据（本轮全部重跑）

| 验证 | 本轮结果 |
|---|---|
| 全量 `npx vitest run` | **270 files passed | 1 skipped；2300 passed | 9 skipped；exit 0；`observer failed` 命中 0 次** |
| `npm run lint` | **exit 0**；tsc 全包通过；`check:pth-boundaries` 违规 0；`check:pth-config` 直读 0 |
| K5 offline（`npx tsx scripts/eval-k5-pilot.ts`） | **138 题；24/24 覆盖；domainRecallAt3=1.0000 / knowledgeRecallAt5=1.0000 / evidenceCoverage=1.0000 / hardNegativePassRate=1.0000 / noAnswerAbstention=1.0000 / mutationScore=1.0000（24/24）** |
| K5 live（testcontainers `postgres:16-alpine` + `scripts/seed-k5-pilot.ts` + `--live`） | **同上全部指标 1.0000；24/24；mutation 24/24 detected** |
| R6 组合套件（`test/pth-composition/r6-acceptance.test.ts`） | **8/8 passed；真实 PostgreSQL testcontainers；无 skip** |
| 各真实 PG 探针 | R1 `memory-store-pg.test.ts` 通过；R2 `knowledge-broker.pg.test.ts` 通过；R3/R4 `knowledge-promotion.test.ts` + `side-effect-outbox.test.ts` + `pg-task-repository.test.ts` 通过（均在全量 vitest 内，真实 PG） |

> 全量 vitest 中唯一 skip 为既有 `test/pth-execution/sandbox-security.integration.test.ts`（9 例，沙箱环境集成套件），
> 不属于 R1–R6 关闭条件，与复验矩阵 P0/P1 无关；其余 2300 例全部通过。

## 5. 关闭条件对账表（对照 `n27-r6-contract.md` §6.2）

| 关闭条件 | 证据 |
|---|---|
| 完整链 `claim → context → commit → outbox → candidate → verification → promotion → retrieve` 真实跑通 | R6 套件 §4.1（`:244`）阶段断言全部通过 |
| 覆盖进程崩溃、并发与跨租户 | R6 套件 §4.2（`:573/:623/:645/:670/:703/:842/:977`）七类故障注入全部负向断言通过 |
| 最终复验报告给出 ACCEPTED/NOT ACCEPTED 与逐项证据 | 本报告 §1/§2 |

## 6. 偏差说明

1. **Redis 未涉及**：本轮全链走 PG 任务仓库/outbox/记忆库，不经过 Redis session/audit 流；按契约
   “如全链涉及”的豁免条件，未引入 Redis 假件。
2. **refiner LLM 边界用确定性 handler 替代**：`§4.1` drainer 消费后的 scoped draft candidate 由
   测试内 handler 以生产 `PgMemoryStore.write` + 生产 provenance/evidence 形状写入；这是组合验收对
   LLM 唯一被替身（deterministic test double）的边界，outbox→candidate 的持久化路径与后续
   verification/promotion/retrieve 全部为真实生产端口。
3. **R6 为验收车道，未夹带任何缺陷修复**；本轮未发现新缺陷。
4. 交付物：`test/pth-composition/r6-acceptance.test.ts`、`scripts/r6-composition-acceptance.ts`、
   本报告；账本更新见 `docs/pth/parallel-lanes.md` 与 `TODO.md`（随同一次 commit）。
